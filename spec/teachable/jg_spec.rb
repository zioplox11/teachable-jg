require 'spec_helper'

describe Teachable::Jg do
  it 'has a version number' do
    expect(Teachable::Jg::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
    expect(false).to eq(false)
  end
end
